apiVersion: apps/v1
kind: Deployment
metadata:
  name: ignored
spec:
  template:
    spec:
      tolerations:
        - key: components.gke.io/gke-managed-components
          operator: Equal
          value: "true"
          effect: NoSchedule
