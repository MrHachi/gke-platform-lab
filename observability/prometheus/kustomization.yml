apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - sts.yml
  - svc.yml
  - node-exporter/ds.yml
  - node-exporter/rbac.yml

configMapGenerator:
  - name: prom-cm
    files:
      - prometheus.yml
